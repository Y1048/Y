# Final PD optimization and simulation-review result — 2026-09-12

Base: `0634844115368a5e910e07891ec2fcacb221f857`.
Tested implementation: `c9aea11d1774a823104d10f662896c5c358cfe5a`.
This report closes the final-review running entry. It does not authorize hardware use.

## Delivered workflow

`tools/RUN_MUJOCO_PD_FINAL.bat` invokes the separate simulator environment.
`mujoco_pd_final.py` provides three explicit modes:

- `optimize`: execute the existing bounded per-joint search, then the final review.
- `finalize`: fully audit an existing per-joint study, freeze its passing vectors,
  run the new final conditions and produce a simulation-only candidate bundle.
- `verify`: reread the original and final evidence, without new dynamics.

The actual execution here used `finalize` against the completed 1,020-case study,
followed by a separate successful `verify` CLI invocation. The original 1,020
integrations were NOT repeated or counted as new simulations. `optimize` routing
was checked with mocked stages; the delegated study and actual finalization were
verified separately. Do not describe that routing check as a new full search.

The old source study's archived source/model inputs remain immutable. The final
review audits those bytes plus gain vectors, torque equations, analytic references,
model mutations, raw traces, all29 guard evidence, plans and frozen selections.
It does not trust old pass flags alone or execute archived code.

## Selected SIMULATION candidate

| Joint | Role | Kp | Kd |
| --- | --- | ---: | ---: |
| 22 | Right shoulder pitch | 100 | 1.4 |
| 23 | Right shoulder roll | 300 | 4 |
| 24 | Right shoulder yaw | 100 | 1.4 |
| 25 | Right elbow | 100 | 1.4 |

**Joint23 Kp300 is a research-only simulator value beyond the unchanged live
Kp100 validator. Do not copy it to VR/G1, enlarge the live validator, or interpret
it as an actuator rating.** All four Kp values lie on their research search caps.
This is not a global or continuous 8-dimensional optimum.

The controller remains pure PD, zero desired velocity and zero feedforward.
No gravity compensation, integral control, target offset, IK damp/cost or live
reference was changed. Wrist and other joint gains were not optimized here.

Both source finalists passed the new conditions. Selection preserves their
original operating minimax order; final outcomes did not retune or rerank them.

| Kp22..25 | Kd22..25 | Prior pass | New final pass | Worst new active RMSE rad | Descriptive worst over216 rad |
| --- | --- | ---: | ---: | ---: | ---: |
| 100,300,100,100 | 1.4,4,1.4,1.4 | 192/192 | 24/24 | 0.011031113650149922 | 0.011962761307210974 |
| 100,300,100,100 | 3,5,3,3 | 192/192 | 24/24 | 0.011236348013604548 | 0.013068196443463197 |

The192 prior cases retain their original active-axis/q22/right7 criteria. The24
new cases add the stricter all-four-proximal-joint endpoint gate. Do NOT claim
that all216 were reevaluated under the new all-four criterion. All four axes are
excited individually in this study, not simultaneously along arbitrary VR paths.
Different condition sets cannot be compared as a hardware improvement percentage.

## New final validation

**48 actual new integrations: two vectors x24conditions, all48 completed and
passed both original and final criteria.** No pruned, missing or failed cells.
Six new model/motor mixtures were fixed before execution, each tested on all
four proximal axes. They include altered amplitudes7/9/11degrees, speeds17/23/27
deg/s, elbow starts +/-6degrees,6 or12repeats, and up to30seconds post-hold.
Mass/inertia, passive damping, friction and torque delay/lag are hypothetical
sensitivities, not measured hardware uncertainties. Exact parameters are frozen
in the raw `manifest.json` and `plan.json`; generator is in the committed code.

Every final endpoint checks ALL four proximal joints, including inactive ones:
position error <=0.02rad and absolute speed <=0.1rad/s over the last100ms of
each hold. The entire last1second of final post-hold is checked. Existing
right7 tail p2p/RMS criteria and all original rejections also remain mandatory.

Selected vector's worst new all-four tail error: **0.0152644561576383rad**.
Selected vector's worst new all-four tail speed: **0.001242734569928266rad/s**.
Alternative vector: error0.015189712549529874rad, speed0.0015185379434500498rad/s.
The metrics are finite-horizon acceptance criteria, not a formal stability margin.

No joint-limit guard events occurred. Across all48 runs the observed minima were:
soft-limit clearance0.2117986012054719rad;
model hard-range clearance0.2617986012054719rad;
stopping-envelope slack0.1605986012054719rad.
Recorded torque-target-limiting and hard-clipping ratios were zero.
Existing0.05rad inner reserve, stopping assumptions and pre/post/final guard
checks were unchanged. No qpos projection, relaxed model limit or substituted
clipped reference was used to improve the score. XML ranges are not measured
mechanical hard-stop positions; simulation refusal is not physical braking.

## Verification actually performed

- New final-review tests:25/25 passed, including actual2-case MuJoCo fixtures,
  missing evidence, inactive-joint bias/speed, entire post-hold window,
  modified source/trace/policy/score/hardware fields and correct refusal.
- Full local Windows suite:282 discovered,281passed,1existing C++ compiler
  dependent skip,0failures/errors. Python3.11.9,MuJoCo3.3.7,NumPy2.4.6.
- Hosted run34629170943, job103361523344 on the tested implementation:
  **282/282 passed,0skips**, including C++ parity. Decoded output inspected;
  runner Ubuntu24.04.5,Python3.11.16,MuJoCo3.3.7,NumPy2.4.6.
  CI ran regression/component dynamics, not the formal48-case final study.
- Source1020 records were reaudited. All48 new raw traces were read back,
  containing1,173,280 rows of29joint states at500Hz and1,392 margin records.
- Standalone `verify` CLI exited0 after rechecking the source and final bundle.
- Python help and optimize routing/no-overwrite were checked. BAT missing-env
  branch correctly exited2, without fallback into the VR environment. The
  configured-environment BAT full simulation was not executed; actual computation
  used the same Python entry point directly.

Initial new-test fixtures produced3 assertion failures: one JSON list/tuple
expectation, and two consequences of asking an exactly1second hold to supply
500sampled points despite50Hz phase alignment. The TEST fixture was changed to
2seconds and the tuple assertion normalized. No controller, formal condition or
acceptance threshold changed. All final formal profiles have at least5second
post-holds. Those prototype failures are not counted as formal study failures.

A later BAT-only change propagates the help command's exit status through a
`:help` label rather than unconditionally returning0. Computational Python
files and frozen simulation inputs are unchanged from the tested code commit.

## Review bundle and provenance

Raw final bundle was completed at:
`C:\Users\user\Documents\G1_PD_Final_20260912`.
Original source study remains:
`C:\Users\user\Documents\G1_PD_PerJoint_20260912`.
Raw final bundle:122files,879,998,674bytes, with source receipt, frozen inputs,
plans,48caseJSON/full-state pairs,simulation_candidate.json andverification.json.
The final bundle still requires its original source study to reproduce verification.

Observed hashes:

- Raw final manifest: `f9d9543d7a7f4ffb5076b9cf325e508de4d25eef83f5aa6a3b95019b875bc28a`.
- Raw simulation_candidate.json: `b3a501fa560e8e26c7284093083203fdfa5be27a608514de04afb39be81f110a`.
- Locally generated48-case comparison CSV: `99466823d74f45727c98ca39890cfd6f7f4879da8f70645a1e93ec59fce50c02`.

The PC connector stopped responding AFTER completed computation, both audits,
export and inspection. This closeout was recorded through the GitHub connector.
Repository `docs/validation/g1_pd_final_20260912/observed_result.json` is a compact
transcription of inspected results, NOT a byte-identical substitute for raw logs.
The48-case CSV/full-state files are confirmed on the PC but are not included in
this connector-fallback upload. Do not claim their complete upload to GitHub.
The isolated PC worktree will not yet include this fallback documentation commit;
read the remote head before continuing and reconcile without resetting the live tree.

## Run and interpretation

See `experiments/twist2_right_arm_manual/MUJOCO_PD_FINAL.md` for installation,
full optimization, reuse of a completed search, verification and exit codes.
`simulation_candidate.json` is deliberately not a live configuration:
`hardware_approved=false`, `recommended_hardware_gains=null`,
`hardware_config_modified=false`, `legacy_hardware_gain_compatible=false`.
A correct audit can verify `no_final_candidate`; it must not invent a vector.

The offline optimization/final-review workflow is implemented and exercised.
Actual hardware finalization is blocked by the unchanged gain bound, unmeasured
servo/latency/torque capabilities, simultaneous VR dynamics, free-base balance,
physical payload/limits/braking, noise/backlash/temperature and SDK/ARM/owner
review. Do not auto-apply this research candidate or relax those checks.
Working VR/Mink/UDP/LowCmd/IK, source model and real gains were not changed;
the original dirty live tree was unchanged at the last successful status check.
No G1 connection,DDS/SDK/publisher/subscriber,motor output or deployment occurred.
