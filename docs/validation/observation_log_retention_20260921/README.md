# Withdrawn feature — rollback completed

The user withdrew automatic log deletion after implementation. PC and G1 active files were restored, and the active helper removed. `rollback.json` is the current state; installation/test evidence here is historical only. Do not reinstall from these artifacts. No real operator logs were deleted.
